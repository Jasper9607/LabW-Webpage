# Lab W Website

Static multi-page website for Lab W.

Preview locally:

```bash
python -m http.server 8000
```

Open `http://localhost:8000/`.

Content files live in `data/`. Update instructions are in `docs/update-guide.md`.
